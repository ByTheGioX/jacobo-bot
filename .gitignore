# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Entorno virtual
.venv/
venv/
env/

# Variables de entorno (nunca subir al repo)
.env

# Configuración con API keys (nunca al repo)
configuracion/

# Fotos procesadas (pesadas, no van al repo)
data/photos/

# Base de datos SQLite (binario, no va al repo)
data/*.db

# Logs sí van al repo para análisis


# macOS
.DS_Store
Proxies/

# Zips del plugin generados por tools/setup_wordpress.py
jacobo-agency-manager-v*.zip

# Archivos de Claude (solo locales, no van al repo)
CLAUDE.md
.claude/
docs/superpowers/
