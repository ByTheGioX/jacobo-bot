# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Entorno virtual
.venv/
venv/
env/

# Variables de entorno (nunca subir al repo)
.env

# Configuración con API keys (nunca al repo)
configuracion/

# Fotos procesadas (pesadas, no van al repo)
data/photos/

# Base de datos SQLite (binario, no va al repo)
data/*.db

# Logs — el bot los reescribe en vivo en el VPS, versionarlos bloqueaba git pull
data/jacobo_bot.log
data/processing_report.jsonl
data/processing_report.log

# Salida de tools/diagnose_wp_listing.py — se regenera en el VPS, mismo problema
data/wp_diagnostic.csv

# macOS
.DS_Store
Proxies/

# Zips del plugin generados por tools/setup_wordpress.py
jacobo-agency-manager-v*.zip

# Archivos de Claude (solo locales, no van al repo)
CLAUDE.md
.claude/
docs/superpowers/
